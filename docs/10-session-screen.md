# 10 セッション画面（統合UI）

## 概要

03〜08 を1画面に統合する本番UI。プレビュー＋自動/手動トリガー＋ストリーミング表示＋読み上げ。

関連: `REQUIREMENTS.md §5.1, §10` / 依存: 03,04,07,08

## Todo

- [×] セッションページ `src/app/session/[id]/page.tsx`（`params` を `await`）
- [×] 映像プレビュー（03）＋カメラ選択を配置
- [×] 自動実況 ON/OFF トグル（04）
- [×] 手動「今の場面について話して」ボタン（04）
- [×] AI発言のストリーミング表示（07 を fetch して逐次表示）
- [×] 読み上げ ON/OFF・ボイス選択（08）と表示テキストの連携
- [×] 直近発言の保持・表示（繰り返し防止用 `recentLines`）
- [×] エラーの画面表示（送信失敗・生成失敗）

## 完了条件

- MVP完成条件（画面取り込み→自動実況→読み上げ）がこの画面で一通り動く。

## 注意

- クライアント状態が多いので `'use client'` は対話部分に閉じ、ページの取得部分は Server Component に。
