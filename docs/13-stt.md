# 13 STT（任意）

## 概要

任意・流用。AIに話しかけたいとき用（「聞いて、今ピンチなんだけど」）。Web Speech API を使う。MVPでは無くても可。

関連: `REQUIREMENTS.md §7.5` / 依存: 10

## Todo

- [ ] Web Speech API（`SpeechRecognition`）で音声入力 `'use client'`
- [ ] 認識テキストを実況リクエストに添えて送信（手動トリガー経由）
- [ ] マイク権限拒否・非対応ブラウザのフォールバック表示
- [ ] STT ON/OFF トグル

## 完了条件

- 話しかけた内容がAIに伝わり、それを踏まえた発言が返る。

## 注意

- 任意機能。MVP の完成条件には含まれない（後回し可）。
- ブラウザ依存（Web Speech 非対応環境では無効化）。
